server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/<YOUR_GITHUB_USERNAME>/<YOUR_CONFIG_REPO>.git
          default-label: main
