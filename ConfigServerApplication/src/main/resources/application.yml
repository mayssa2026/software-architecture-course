server:
  port: 8888

spring:
  application:
    name: ConfigServerApplication
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mayssa2026/software-architecture-course
          search-paths:
            - lab10
