spring:
  application:
    name: product-service

  cloud:
    consul:
      host: localhost
      port: 8501
      discovery:
        register: true
        service-name: product-service

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health
